{
  "name": "ai-cloud-tracker",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "pnpm -r --filter=./packages/* build",
    "compile": "pnpm -r --filter=./packages/* compile"
  },
  "devDependencies": {
    "typescript": "5.7.3"
  }
}
